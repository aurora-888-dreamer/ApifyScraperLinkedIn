{
  "title": "LinkedIn Public Jobs Scraper Input",
  "description": "Input for the LinkedIn jobs scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Search query",
      "type": "string",
      "description": "Keyword to search for jobs"
    }
  }
}
